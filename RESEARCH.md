# QR-AirBridge · 技术调研与迭代路线

> 配合 `ANCHOR.md`（项目现状）使用。本文是讨论结论的固化；
> §8 #1（v1.0）、#3（v1.1，按实测修正后落地）已实施，实施状态与验证数据以 `ANCHOR.md` 为准。
> 最后更新：2026-08-05（#7 v1.5.0~v1.5.2（真机结论：用户双端不可用，保留给外部 GMS/桌面用户）；
> #9 ROI 跟踪 v1.6.0；#10 换帧门控 + #5b 帧同步信标 + 默认档上移 30FPS/650B = v1.7.0（真机复测：#5a 对焦锁定证伪）；
> **v1.7.1 = #5a 回退 + #9×#10 矩形冲突修复 + 测试辅助两项（已真机复测：iPhone ≈24KB/s 光学层封顶、
> 安卓 ≈10KB/s 解码受限）**，见 §5/§6/§8；**#8 Worker 池开工条件已触发，待用户拍板**）

## 1. 问题定义

领域学名 screen-camera communication（视觉数据通道）。
现状：小文件体验好；大文件受限于速率与稳定性。
当前基线：450B 单帧 @ 3FPS ≈ 1.3 KB/s 净载（v0.9 真机实测）。

设计原则（用户明确要求）：**速度与质量并存**——
不为提速牺牲完整性（任何加速必须保留帧级 CRC32 + 文件级 SHA-256 全量校验兜底），
也不为稳妥接受无谓冗余（能靠更聪明的码率分配省掉的开销一律省掉）。

吞吐公式（一切优化都归到这三个因子，再减去开销）：

```
有效吞吐 = 单帧净载 × FPS × 解码命中率 − 协议开销
```

瓶颈拆成五个维度：**协议开销、符号层密度、擦除码效率、解码管线、光学时序**。
（最初设想的"编码/纠错/解码速度"三条，补充了协议开销与光学时序——这两者是当前 ROI 最高的。）

## 2. 协议开销（最便宜，先做）

现状账本（450B 帧）：base64 亏 33%（600 字符装 450B），META 每帧 80~100B，
合计开销约 35%，净载只有六成。

- **base64 → base45（RFC 9285）**：QR alphanumeric 模式 2 字符存 11 bit；
  base45 三字符装 2 字节，塞进 alphanumeric 效率约 97% vs base64-in-byte 75%，
  载荷多约 29%，且全程文本、无解码兼容性问题。TID/SEED/CRC 本就在 alphanumeric 字符集内，整帧可切。
- **更激进：二进制直传**：定长帧头替代分隔符，省掉全部文本编码膨胀。
  风险：解码端必须拿到原始字节而非被 UTF-8 猜测过的字符串——内嵌 zxing-js 不可靠，
  zxing-wasm 可以（见 §5）。先做 base45，二进制留作备选。
- **manifest 瘦身**：文件名/SHA/参数做成喷泉流 block 0（或独立周期帧），
  数据帧只留 TID+SEED+payload+CRC。

预期：不改任何"硬"部分，吞吐 +50% 左右。

> **实施状态（v1.0 已落地）**：base45 + 定长无分隔符头（TID8/SEED6/TOTAL4/CRC6）+
> 清单入喷泉块 0（每 32 帧重发）+ 接收端 SEED 去重，全部按本条实施；
> 帧比特数实测 -35%（450B 块：v1 约 734 byte 字符 → v2 699 alphanumeric 字符）。

## 3. 擦除码效率（喷泉码本身）

- **双层纠错是已被验证的路线**（用户侧亦确认有项目在用 Reed-Solomon + fountain 组合）：
  帧内 RS 纠"错"（把废帧救活）+ 帧间喷泉纠"丢"（缺块补洞）——这正是"速度与质量并存"的架构表达：
  喷泉免重传保速度，RS 帧内纠错保质量。QR 方案里帧内角色由 QR 自带 RS 扮演（强度固定不可调）；
  cimbar 把它做成显式可调的 `--ecc` 参数。演进方向：保持双层结构，帧内纠错强度参数化。
- **度分布**：当前是自研经验值（40%/30%/20%/10%），非 LT 理论上的 Robust Soliton。
  度分布决定译码开销 ε（需收 K+ε 包解 K 块）；拍脑袋分布 ε 可能 20~50%。
  换 Robust Soliton 公式，几十行 JS。
- **现成码**：
  - `catid/wirehair`（C++，可编 WASM）：平均 K+0.02 包即可译码，几乎贴理论极限；cimbar 在用。
  - RaptorQ（RFC 6330）：收到 K+h 包恢复概率 1−1/256^(h+1)；Rust 实现 `cberner/raptorq`。
    注意：RaptorQ 历史上有 Qualcomm 专利声明，个人/研究可，商用需查。
- **系统码（systematic）**：先发一整轮原始块（全度1）再随机组合。
  小文件常第一轮收齐，进度条线性增长；wirehair 前 N 块天生是原始数据。
- **帧级 CRC32 保留**：防 QR 自带 RS 静默误纠，开销可忽略。
- **QR 纠错 M(15%) → L(7%)**：CRC+喷泉已兜底，可换约 10% 容量；命中率会掉，实测找平衡。

> **实施状态与实测结论（v1.1）**：
> - Robust Soliton（c=0.1, δ=0.5）**已落地，效果显著**：从头解码 ε 实测 0.03~0.07
>   （经验分布对照实测 0.2~0.5，证实了本条担心），且对 0~30% 丢帧几乎免疫。
> - 系统码第一轮：**实测后否决，未采用**。它只在 0% 丢帧时恰 L 帧收齐（省 ~5 帧）；
>   一旦有损，丢失的系统帧形成"洞"，尾部回补成本高于纯喷泉全程
>   （L=70、15% 丢帧：系统轮 100 帧 vs 纯 Soliton 75 帧）。真实信道必有损，故不采用。
>   进度条线性的好处随之放弃（peeling 雪崩，进度前慢后陡），待真机观察 UX。
> - c/δ 当前取 0.1/0.5，留待 §8 #4 按真实信道调优。
> - 另测得：PRNG 首输出对小种子非均匀，随机种子必须用大值（生产已是 [L, 36^6) 均匀随机）。

## 4. 符号层密度（每帧塞多少）

- 膝点实测：单帧越大命中率越低。用 txqr-tester 式自动扫描（FPS × 块大小 × 距离矩阵）找膝点，
  不要凭感觉调。
- 换制式：Aztec / DataMatrix 无大定位角、静区小，同面积密度更高；zxing-wasm 均支持。
- 远期质变：彩色网格（cimbar 路线）——位置+颜色每 tile 编多 bit，单帧纠错后约 7.5KB，
  直接跨数量级；代价是白平衡/色彩漂移校准，解码管线基本重写，JS 生态无现成解码器。
  用户侧亦确认彩色码有现役实现（libcimbar 即彩色 tile），此路线不是理论空想，
  但投入最大，排在迭代表末位，先把 QR 路线的潜力吃干榨净再评估。

## 5. 解码管线（当前是 JS 天花板，不是相机天花板）

- **zxing-wasm**（`Sec-ant/zxing-wasm`，MIT，活跃维护）：ZXing-C++ 的 WASM 版，
  快 3~10 倍，抬高 FPS/码密度上限；还解锁二进制模式与 Aztec/DataMatrix。（**v1.3 已落地**：reader-only 内嵌，无头实测 18/18 全解码）
- **Web Worker 解码**：现在 rAF 同步解码卡 UI 又浪费相机 30fps 输入；
  主线程采帧、worker 解码、忙则丢帧。（**v1.3 已落地**：Blob Worker + transferable + pull 模型）
- **重复帧去重**：3FPS 发送 = 同码被扫约 10 次，按帧内 SEED 去重，不进消元。
  （**v1.0 已落地**：接收端 seenSeeds 去重 + 统计「重复」计数。）
- **ROI 跟踪**：命中后记四角坐标，后续只裁该区域，丢失再全帧扫——比固定中央 75% 裁剪聪明。
  （**v1.6.0 已落地并验收**：原生档 boundingBox / wasm position 归一回传，roiTrack 状态机（±35% 邻域、连丢 3 帧回退、
  深度通道再捕获），三级同步链不接；无头 42 断言 + Electron 真回环全绿；
  与 worker 池是乘法关系：单次解码成本降一个量级 → 同算力每秒试更多次）

> **瓶颈分型定论（2026-08-04，v1.4.0 拉满复测，讨论固化）**：
> - **安卓 = 解码算力瓶颈**：worker 档 ~10/s，1300B@50FPS 拉满仍 ~10/s（供给吃不满）。
>   decode 侧四项优化全部直接见效，ROI 序 = BarcodeDetector → ROI 跟踪 → Worker 池 → 换帧检测。
> - **iPhone = 供给/光学瓶颈**：主线程档 50/s 已超 36KB/s 供给；FPS 拉满 50 反降至 ~30/s（换帧混叠主导）。
>   先做 §6 光学（参数锁定/帧同步信标/避开整数比 FPS）；decode 侧优化要等光学把供给抬过解码上限后才兑现。
>   前提注：worker 池在 iPhone 上仅 https worker 档可用（file:// 主线程档用不上池）。
> - 用户战略备注：decode 侧吃干榨净后即可专心攻编码/发射端，不必两端同步改；
>   ROI/换帧检测省下的 CPU 同时降发热耗电，长时间扫大文件体验受益（与吞吐无关的隐性收益）。
> - **BarcodeDetector 原生档（第零档，已实现未发布，待安卓真机复测）**：安卓 Chrome 的 Shape Detection API，硬件级 QR 检测，
>   比 wasm 快一个量级。`'BarcodeDetector' in window` + `getSupportedFormats()` 含 qr_code 两步探测即用，
>   不支持（iOS）静默落回现有三级链，detect 连续 3 次异常同样退回。
>   v2 帧为纯 alphanumeric 文本，rawValue 字符串正好够用；零 wasm 体积成本。
> - **Worker 池 ×2（未做，v1.7.0 后定夺）**：wasm 解码单核，手机 6~8 核闲置；双 worker 轮转，安卓 ~10 → ~20/s。
>   改造点 = 「忙则丢帧」单忙位 → 池调度。iPhone 仅 https worker 档受益。
>   安卓 ROI+门控后的真机解码率决定此项是否还值中成本。
> - **换帧检测门控（✅ v1.7.0 已落地）**：采帧后 16×16 降采样亮度签名比对，同帧跳过——
>   v1.3.2 实测每帧平均重复解码 1.7 次，这部分预算全让给新帧；逃逸阀（连跳 15 次强制送解）防误判死锁。
>   与 #5b 信标（换帧混合帧检测）同闸，见 §6。

## 6. 光学时序（稳定性的真凶）

- **拍频条纹**：60Hz 屏 vs 非整数倍曝光 → 滚动暗带；曝光锁 1/120s 倍数或开防闪烁。
- **换帧混叠**：换帧瞬间相机正在曝光则该帧必废。FPS 避开相机帧率简单整数比；
  高级做法：角落小块黑白交替作帧同步信标，接收端检测到切换丢一帧（cimbar 有类似机制）。
- **相机参数锁定**：`getUserMedia` advanced constraints 手动锁
  `exposureMode`/`focusMode`/`whiteBalanceMode`（安卓 Chrome 支持较好）；自动曝光对白底黑码会飘。
- 全白背景易过曝糊边，可试 `#e8e8e8` 灰白。多尺度轮换方向正确（v1.2 起为随档位比例轮换 CAP_SCALES），保留。

> **实施状态（v1.7.0 已落地，待真机复测）**：
> - **帧同步信标（#5b）✅**：信标条不放在角落（接收端对准中央框，角落可能出画），而是贴在码下静区外
>   （drawQR 画布底部全宽 × 5 模块，随帧序号黑白翻转）。接收端已知码位置（ROI）时采码下窄带，
>   8 行行均值以最亮行归一：含黑行或全带均匀 = 纯净放行，中间灰 = 混合帧跳过——行剖面归一对
>   pos 语义（zxing-cpp 回传的是码角 bbox，非定位中心，Electron 真回环抓出并修正）、模块数、曝光漂移免疫。
>   旧发送端无条 = fail-open（可混发收）；横转姿态经逃逸阀退化不死锁；方向自适应留作后续。
> - **相机参数锁定（#5a）❌ 真机证伪，v1.7.1 已回退**：原案 = 首次解码命中时锁 `focusMode:'single-shot'`。
>   真机复测（2026-08-05）：single-shot 在安卓 Chrome 真实生效，但锁定时机（首次命中，手机常仍在移动对准）
>   + 锁死后手持微距变化无法重对 → 聚焦异常、ROI 振荡、解码 <5/s；iPhone Safari 无该能力本就不生效——
>   功能等于只伤害安卓。教训：连续对焦对「固定距离拍屏幕」场景本就胜任，不锁优于错锁。
>   曝光/白平衡不泛化——各机行为差异大、手动值无通用安全默认，真机有明确痛点再议。拍频条纹与灰白背景未动（静区白度不可污染）。

## 7. 架构层远期选项（知道即可，暂不做）

- **反向 ACK 通道**：接收端手机把"已收块位图"以 QR 显示在自己屏幕，发送端 PC 用 webcam 读回
  → hybrid ARQ，喷泉冗余压到近零。复杂度大增，发送端无摄像头时退化回纯喷泉。
- **彩色网格**：见 §4，数量级提升但重写解码端。

## 8. 迭代顺序（ROI 排序）

| # | 内容 | 预期收益 | 风险/成本 | 状态 |
|---|---|---|---|---|
| 1 | base45 + manifest 瘦身 + SEED 去重 | 约 +50% | 低，纯 JS | ✅ v1.0 已落地（帧比特 -35% 实测） |
| 2 | zxing-wasm + Worker 解码管线 | FPS/密度上限↑ | 中，嵌 WASM（实测 +1.42MB） | ✅ v1.3 已落地（无头全绿；**真机复测通过**：900B@24FPS 解码 ~10/s，v1.2 同期个位数/s）。Aztec/二进制帧/彩色网格前置已就位 |
| 3 | Robust Soliton 度分布（系统码第一轮实测后否决） | ε 0.2~0.5 → 0.03~0.08 | 低，几十行 | ✅ v1.1 已落地（按实测修正） |
| 4 | txqr-tester 式参数自动扫描（含 Soliton c/δ、单帧膝点） | 找到设备膝点 | 中，做测试工装 | ✅ 手工扫描完成（2026-08-03）：FPS 膝点≈20（换帧混叠）、单帧膝点≤650（接收端 ≤640px 成像限制）；默认值定 FPS 8/单帧 450。自动化工装缓做；单帧膝点的成像限制 → v1.2 分辨率阶梯已缓解（真机复测完成，v1.3 wasm 解码后进一步解除） |
| 5 | 相机参数锁定 + 帧同步信标 | 稳定性收尾（**iPhone 主菜**：唯一能把供给抬过 36KB/s 的手段） | 中，兼容性分支多 | 信标 #5b ✅ v1.7.0 已落地；**对焦锁定 #5a ❌ 真机证伪、v1.7.1 回退**（锁定时机+手持微距变化矛盾，iPhone 本无该能力）；曝光/白平衡不泛化 |
| 6 | 彩色网格 / 反向 ACK | 数量级 | 高，远期 | 未做 |
| 7 | BarcodeDetector 原生第零档（安卓 Chrome 硬件解码） | 安卓解码或 ×数倍 | 低，几行探测码，零体积 | ✅ v1.5.0~v1.5.2 已发布；**真机结论：用户双端不可用（安卓 Chrome 无qr_code 缺后端、iPhone Safari 无API）**，功能保留给外部 GMS/桌面用户（桌面 macOS Chromium 实测 50/50 全解码），skip 显示收敛；wasm 链仍是用户主力 → #9/#8/#10 按序 |
| 8 | Worker 池 ×2（多核并行解码） | 安卓 ~10 → ~20/s | 中，忙则丢帧改池调度 | 未做；**v1.7.1 复测触发预定开工条件**（安卓 ROI+门控后仍 ~10/s 解码受限），待用户拍板；iPhone 已在光学层封顶（~20/s 纯净帧），无收益 |
| 9 | ROI 跟踪（命中后裁小区域，丢失回退） | 单次解码成本 ↓量级 | 中 | ✅ v1.6.0 已落地并验收（无头+Electron 真回环全绿；与 #8 乘法叠加） |
| 10 | 换帧检测门控（16×16 粗网格差分，图未变跳过） | 省 ~1.7× 重复解码预算 | 低 | ✅ v1.7.0 已落地（含逃逸阀；与信标同闸，统计行加「跳过 n/s」） |

> #7~#10 为 2026-08-04 讨论定案的 decode 侧路线：**安卓按 #7→#9→#8→#10 的 ROI 序做**
> （解码算力是其瓶颈）；**iPhone 先做 #5 光学**，decode 侧收益待供给抬过解码上限后兑现。
> 全部做完后重心转编码/发射端（用户战略备注，§5）。
> **2026-08-05 进度**：#5b 信标与 #10 已合入 v1.7.0（用户拍板，默认档同步上移 30FPS/650B）；
> 真机复测 #5a 对焦锁定证伪（安卓锁死/拉风箱、iPhone 本无该能力）→ **v1.7.1 回退 #5a**，
> 同版修复 #9×#10 矩形冲突（ROI 裁剪 16px 网格对齐，harness 对照：微抖下旧公式矩形变 19/20、新对齐 0/20）；
> decode 侧仅剩 #8，是否开工看 v1.7.1 真机复测数据。

每项改动前后用同一基线对比：固定 100KB / 1MB 测试文件，记录完成时间与命中率。
（真机基线对比尚未做——v0.9/v1.0/v1.1 三份备份页待同机同距离实测。）

## 9. 参考项目

- https://github.com/sz3/libcimbar — 领域天花板。彩色 tile + 帧内 RS + wirehair + zstd，
  单帧约 7.5KB（纠错后），持续 ~850kbps（≈106KB/s）。协议/帧同步/色彩校准见 DETAILS.md；
  cimbar-bigfile 有大文件分包 + manifest SHA 重组思路。
- https://github.com/divan/txqr — 同代技术（动画 QR + LT）。重点抄 `cmd/txqr-tester`
  的参数扫描方法论；作者 fountain codes 博客实验记录值得读。
- https://github.com/catid/wirehair — 喷泉码实现；旁有 https://github.com/catid/leopard （O(N log N) RS 擦除码）。
- https://github.com/cberner/raptorq — RFC 6330 Rust 实现，作者博客有完整优化手记。
- https://github.com/Sec-ant/zxing-wasm — 解码端替换件（QR/Aztec/DataMatrix，支持读写字节）。
- https://github.com/BlockchainCommons/bc-ur — 硬件钱包动画 QR 传签名的生产级协议，
  fountain 分片 + 校验的规格书干净，适合参考协议分层。
- RFC 9285（base45）、RFC 6330（RaptorQ）。
